<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Economik0 - Plataforma de comercio electrónico que conecta productores locales con consumidores. Sin intermediarios.">
    <title>{% block title %}Economik0{% endblock %}</title>
    
    {% block stylesheets %}
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;800&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    {% endblock %}
</head>
<body>
    {# ===== SKIP LINK - Accesibilidad WCAG 2.1 AA ===== #}
    <a href="#main-content" class="skip-link visually-hidden-focusable">
        Saltar al contenido principal
    </a>

    {# Fondo decorativo (aria-hidden para lectores de pantalla) #}
    <div class="bg-wrapper" aria-hidden="true">
        <div class="light-orb"></div>
    </div>
    
    {# Cabecera con navegación #}
    <header>
        {{ include('partials/_navbar.html.twig') }}
    </header>

    {# Contenido principal con landmark accesible #}
    <main id="main-content" class="d-flex align-items-center justify-content-center" style="min-height: 90vh;" tabindex="-1">
        {# Región de alertas para lectores de pantalla #}
        <div id="flash-messages" aria-live="polite" aria-atomic="true" class="visually-hidden">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    {{ message }}
                {% endfor %}
            {% endfor %}
        </div>
        
        {% block body %}{% endblock %}
    </main>

    {# Pie de página #}
    <footer class="visually-hidden">
        <p>© {{ "now"|date("Y") }} Economik0. Todos los derechos reservados.</p>
    </footer>

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% endblock %}
</body>
</html>